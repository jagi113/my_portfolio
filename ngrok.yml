version: 2
authtoken: ${NGROK_AUTHTOKEN}

tunnels:
  portfolio:
    addr: portfolio-nginx:80 # for connecting with nginx container on the same network use inner port (not outer)
    labels: 
      - edge=edghts_2aaBTu6pO7FGyyngo2xEjAtMaCi

